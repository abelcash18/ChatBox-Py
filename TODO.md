# TODO - chatbox

- [x] Create project scaffold (FastAPI + static HTML/JS frontend)
- [x] Add simple “RAG” pipeline: load local docs, embed with lightweight method, retrieve top chunks
- [x] Implement mock LLM (or optional OpenAI integration via env var) to generate replies
- [x] Add API endpoints: POST /api/chat and GET /
- [x] Add basic tests (pytest) for retrieval + API smoke test

- [x] Add README with run instructions


